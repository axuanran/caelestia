general {
#    layout = dwindle
    layout = scrolling

    allow_tearing = false  # Allows `immediate` window rule to work

    gaps_workspaces = $workspaceGaps
    gaps_in = $windowGapsIn
    gaps_out = $windowGapsOut
    border_size = $windowBorderSize

    col.active_border = $activeWindowBorderColour
    col.inactive_border = $inactiveWindowBorderColour
}
scrolling {
    fullscreen_on_one_column = true
    column_width = 0.9
    direction = right
}


dwindle {
	preserve_split = true
	smart_split = true
	smart_resizing = true
}
animations {
    enabled = 0

    # # 贝塞尔曲线定义（速度快、起步猛）
    # bezier = instant, 0, 0, 1, 1
    # animation = windows, 1, 1.5, instant
    # animation = workspaces, 1, 1.5, instant


}